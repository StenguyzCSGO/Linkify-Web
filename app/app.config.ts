export default defineAppConfig({
	ui: {
		colors: {
			primary: 'cyan',

		},
    card: {
      slots: {
        root: 'group hover:ring-primary/50 hover:scale-105 transition-all duration-300'
      }
    },
	}
})
